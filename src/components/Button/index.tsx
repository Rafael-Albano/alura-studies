import { Component, ReactNode } from "react";

// CLASS COMPONENTS.
class Button extends Component {
    render(): ReactNode {
        return(
            <button>Save</button>
        )
    }
}

export { Button }